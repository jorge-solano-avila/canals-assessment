# Config lives here rather than in pyproject.toml: this repo has no pyproject,
# and adding one purely for tool config sits awkwardly beside the pipenv setup.
[mypy]
python_version = 3.12
strict = True
warn_unreachable = True
show_error_codes = True
